# 实施计划导航

_状态：当前计划索引 | 更新：YYYY-MM-DD_

`docs/plans/` 保存多步骤、高风险或跨模块变更的当前实施计划。它不是任务状态板；任务状态仍以 `docs/TASKS.md` 为准。

## 何时需要计划

- 跨多个模块、仓库或团队；
- 涉及公共契约、数据迁移、权限、安全或部署；
- 存在明显方案取舍、兼容窗口或回滚要求；
- 预计需要多个独立验证阶段或交接。

小型、局部、低风险且可一次验证的改动可以直接实施，但仍需满足项目验证标准。

## 大型计划确认门

大型计划包括用户明确提出的大规划、roadmap 或多阶段交付，也包括高风险契约、迁移、安全、发布或恢复边界的变更。其他任务若同时出现跨两个以上模块或所有权边界、三个以上顺序阶段、多个独立验收结果、无法在一个专注会话内完成等至少两个信号，也按大型计划处理。

固定流程如下：

1. 只读核对现状，自动拆成 `2-6` 个有序、可独立验收的切片。
2. 在对话中列出每个切片的目标结果、范围、依赖、验收方式和回退点，状态设为 `awaiting_user_confirmation`。
3. 等用户批准或调整拆分；确认前不开始实现、不创建交付 PR，也不改变外部状态。
4. 确认后使用 [TEMPLATE.md](TEMPLATE.md) 落盘，状态改为 `approved`，同一时间只允许一个切片为 `in_progress`。
5. 当前切片完成验证并记录证据后推进下一个；只有范围、顺序、接口、迁移或风险发生实质变化时才重新确认。

切片应交付一个可观察结果，并能在一次专注开发会话内完成和验证。默认整个计划使用一个交付分支和一个 PR；只有切片可以独立发布、需要不同回滚窗口或必须隔离高风险时才拆成多个 PR。

## 计划索引

| 计划 | 状态 | 关联任务/设计 | 当前阶段 | 更新时间 |
|---|---|---|---|---|
| <!-- plan.md --> | draft/approved/in-progress/complete/superseded | <!-- links --> | <!-- phase --> | <!-- date --> |

## 维护规则

- 计划必须有范围、非范围、证据基线、步骤、验收、风险、迁移/回滚和完成定义。
- 大型计划必须记录用户确认状态和确认后的切片版本；`awaiting_user_confirmation` 不得进入实现。
- 每次只允许一个切片为 `in_progress`，不得在结束时一次性把多个未记录过程的切片全部标为完成。
- 实施中发现事实变化时更新计划，不保留已经失真的步骤作为当前指令。
- 完成后把稳定事实同步到架构/摘要，把可重复经验同步到 `project-memory/`。
- 旧计划标记为 superseded 或归档，不与当前计划并行充当权威源。

新计划从 [TEMPLATE.md](TEMPLATE.md) 复制。
